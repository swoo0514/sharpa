import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // 앱 컴포넌트를 가져옵니다.

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
